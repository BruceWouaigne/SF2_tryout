{
{% for customer in customers %}
	"customer_{{ loop.index }}" : {"first_name" : "{{ customer.firstName }}", "last_name" : "{{ customer.lastName }}", "email_address" : "{{ customer.emailAddress }}"}{% if loop.last == false %},{% endif %}
{% endfor %}
}